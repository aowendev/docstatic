---
title: Example Page
description: A React page that can be edited in the CMS with JSX, HTML and MDX content. 
---

<Passthrough type="html" string="
  <div class='theme-doc-markdown markdown'>
    <header>
      <h1>Example page</h1>
    </header>
  </div>
" summary="HTML Content" />

This example page imports the React component `ColorGenerator`.

You can use it to create generate a color set for your site.

<Passthrough type="jsx" string="import ColorGenerator from '@site/src/components/ColorGenerator';" summary="Import Statement (hidden)" />

<Passthrough type="jsx" string="<ColorGenerator />" summary="JSX Component" />
