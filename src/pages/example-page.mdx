---
title: Example Page
description: A React page that can be edited in the CMS with JSX, HTML and MDX content. 
---

<Passthrough string="<h1>Example page</h1>" summary="HTML" />

This example page imports the React component `ColorGenerator`.

You can use it to create generate a color set for your site.

<Passthrough string="import ColorGenerator from '@site/src/components/ColorGenerator';" summary="JSX" />

<Passthrough string="<ColorGenerator />" summary="MDX" />
