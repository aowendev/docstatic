---
help: null
lastmod: '2026-06-01T00:00:00.000Z'
title: Notifications
tags:
  - software
  - software_notifications
  - software_notifications_email
  - software_notifications_teams
  - software_notifications_slack
draft: false
review: false
translate: false
approved: false
published: false
unlisted: true
---

# 通知

docStatic はワークフロー通知の送信に [GitHub Actions](https://docs.github.com/en/actions) を使用しています。レビューと翻訳の通知例は `.github/workflows/` フォルダに含まれています。これらを有効にするには、GitHub での設定が必要です。また、独自のカスタム通知を開発するための出発点としても使用できます。

## メール

`email-review.yml` と `email-translate.yml` アクションは、メインブランチの Markdown ファイルにレビューまたは翻訳タグが設定されたときにトリガーされます。使用するには、以下の[シークレット](https://docs.github.com/en/actions/how-tos/write-workflows/choose-what-workflows-do/use-secrets)をリポジトリに追加してください：

* `SMTP_SERVER`
* `SMTP_PORT`
* `SMTP_USERNAME`
* `SMTP_PASSWORD`
* `NOTIFY_EMAIL_TO`
* `NOTIFY_EMAIL_FROM`

この情報はメールプロバイダーから取得できます。たとえば：

* [iCloud](https://support.apple.com/en-us/102525)（[アプリ専用パスワード](https://support.apple.com/en-us/102654)が必要です）。
* [Outlook](https://support.microsoft.com/en-us/office/pop-imap-and-smtp-settings-for-outlook-com-d088b986-291d-42b8-9564-9c414e2aa040)

## Slack

`slack-review.yml` と `slack-translate.yml` アクションはメールバージョンと同じトリガーを使用します。使用するには、Slack の Webhook URL を `SLACK_WEBHOOK_URL` という名前のシークレットとして追加してください。詳細については、Slack ドキュメントの [Sending messages using incoming webhooks](https://docs.slack.dev/messaging/sending-messages-using-incoming-webhooks/) を参照してください。

## Teams

`teams-review.yml` と `teams-translate.yml` アクションは Slack バージョンと同様に動作します。Microsoft は 2025年12月から Teams の **Incoming Webhooks** アプリを廃止しますが、現在のところ Adaptive Cards をワークフローに送信するための公開された GitHub メソッドは存在しません。回避策として curl を使用します：

```yaml
- name: Send Teams test notification
  run: | 
    curl --ssl-no-revoke -X POST \
    --location ${{ secrets.MS_TEAMS_HTTP_POST_URL }} \
    --header 'Content-Type: application/json' \
    --data '{ \
      "type": "message", \
      "attachments": [{ \
        "contentType": "application/vnd.microsoft.card.adaptive", \
        "content": { \
          "$schema": "http://adaptivecards.io/schemas/adaptive-card.json", \
          "type": "AdaptiveCard", \
          "version": "1.3", \
          "body": [ { \
            "type": "TextBlock", \
            "text": "This is a test notification from GitHub Actions (Teams).", \
            "wrap": true }]}}]}'
```

1. Teams で、メッセージを送信したいチャンネルに移動し、省略記号（**…**）をクリックして **Workflows** を選択します。
2. **Manage** をクリックします。
3. **New Flow** をクリックします。
4. Webhook のテンプレートを検索して **Send webhook alerts to a channel** をクリックします。
5. （オプション）**Flow name** を編集します。
6. サインインして **Next** をクリックします。
7. リストから **Team** と **Channel** を選択して **Create Flow** をクリックします。
8. `MS_TEAMS_HTTP_POST_URL` をコピーして GitHub のシークレットとして保存し、**Done** をクリックします。

<Admonition type="tip" title="ヒント">
  テスト通知の GitHub Action を使用して接続をテストできます。
</Admonition>

<RelatedTopics maxResults={7} />
